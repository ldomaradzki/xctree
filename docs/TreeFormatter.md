# TreeFormatter

---

## Topics

### Structures

**`struct PrintableElement`**
Element data to print in tree format.

> **`init(role: String?, label: String?, title: String?, value: String?, identifier: String?, hint: String?, traits: [String]?, children: [PrintableElement])`**
>
**`struct TreeNode`**
Represents an accessibility tree node for JSON serialization.

> **`init(from: any Decoder) throws`**
> Inherited from  .
>
> **`init(role: String?, label: String?, title: String?, value: String?, identifier: String?, hint: String?, traits: [String]?, frame: TreeNode.FrameInfo?, children: [TreeNode])`**
>
**`struct TreePrinterConfig`**
Configuration for tree printing.

> **`init(maxWidth: Int, useColors: Bool)`**
>

### Enumerations

**`enum ColorSupport`**
ANSI color code support for terminal output.

> **`reset: String`**
> ANSI color codes
>
> **`static func stripColors(String) -> String`**
> Strips all ANSI color codes from a string.
>
> **`static func visibleLength(String) -> Int`**
> Calculates the visible length of a string (excluding color codes).
>
**`enum JSONFormatter`**
Formats accessibility data as JSON.

> **`static func format(TreeNode) -> String`**
> Encodes a tree node to pretty-printed JSON string.
>
**`enum TextWrapper`**
Handles text wrapping with indentation support.

> **`static func print(String, prefix: String, maxWidth: Int, color: String)`**
> Wraps text with indentation and prints to stdout.
>
> **`static func wrap(String, prefix: String, maxWidth: Int, useColors: Bool) -> [String]`**
> Wraps text to fit within a maximum width while preserving indentation.
>
**`enum TreePrinter`**
Prints accessibility trees with visual connectors and formatting.

> **`static func print(PrintableElement, depth: Int, isLast: Bool, prefix: String, config: TreePrinterConfig)`**
> Prints an element and its children as a tree structure.
>
> **`static func printRoots([PrintableElement], config: TreePrinterConfig)`**
> Prints multiple root elements as siblings.
>

---

### JSONFormatter.format(_:)

**Summary:** Encodes a tree node to pretty-printed JSON string.

##### Return Value

JSON string, or error message if encoding fails

---

### ColorSupport.stripColors(_:)

**Summary:** Strips all ANSI color codes from a string.

##### Return Value

The text with all color codes removed

---

### ColorSupport.visibleLength(_:)

**Summary:** Calculates the visible length of a string (excluding color codes).

##### Return Value

The visible character count

---

### TextWrapper.wrap(_:prefix:maxWidth:useColors:)

**Summary:** Wraps text to fit within a maximum width while preserving indentation.

##### Return Value

Array of wrapped lines

---

---

*Generated by docc2md - DocC Archive to Markdown Converter*
*For AI coding agents and documentation processing*
